{
  "name": "healthicons-dotnet-tools",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "preprocess": "node preprocess-svgs.mjs",
    "generate:fonts": "node generate-fonts.mjs",
    "generate:csharp": "node generate-csharp.mjs",
    "generate": "npm run preprocess && npm run generate:fonts && npm run generate:csharp"
  },
  "dependencies": {
    "fantasticon": "^3.0.0",
    "healthicons": "^2.0.0",
    "svg2ttf": "^6.0.3",
    "svgicons2svgfont": "^12.0.0",
    "svgo": "^3.3.2"
  }
}

